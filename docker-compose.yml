services:
  backend:
    build:
      context: .
    restart: unless-stopped
    ports:
      - "5050:5050"
    volumes:
      - portfolio-app:/portfolio_app
    networks:
      - backend_network

networks:
  backend_network:


volumes:
  portfolio-app:
